{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "buildCommand": "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile && pnpm prisma generate && pnpm build",
    "installCommand": "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile",
    "devCommand": "pnpm dev"
}