{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "buildCommand": "turbo build --filter=dashboard",
    "installCommand": "pnpm install",
    "devCommand": "pnpm dev",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}